import React from 'react';
import { useMetamanGame } from '../lib/stores/useMetamanGame';

export default function MetamanCooldownIndicator() {
  // Remove the red circle cooldown indicator - the clock bar in the HUD is enough
  return null;
}